using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class KitaiManager : MonoBehaviour
{
    private int kitai_num = 0;
    public GameObject[] kitaiObj;

    // Start is called before the first frame update
    void Start()
    {
        /*
        for(int i = 0; i < kitai_num; i++)
        {
            kitaiObj[i] = kitai_num[i];
        }

        switch (kitaiObj)
        {
            case 0:
                break;

        }
        */
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
